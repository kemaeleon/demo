{% load static %}
{% load django_tables2 %}
{% load bootstrap3 %}
{% load render_table from django_tables2 %}

<!doctype html>
<html>
	{% include "head.html"%}
<body>
<div class="container">
{% block body %}
<div class="row">
<div class="col-sm-6">
<img src="{% static "assays/static/logo.png" %}" alt="lessons from viruses">
</div>
</div>


<div class="row" >
    <h1> Regulatome Database - Development Version </h1>
    This server is currently under active development, its purpose is to allow feedback.  
   </div>  
    <div class="row">
            <div class="col-sm-12" id="gene_selection">
       <h2> Filter Measurements and Plot Data</h2>
                <div class="pull-right">
                    {% if view.export_formats %}
                        {% for format in view.export_formats %}
                        <a href="{% export_url format %}" class="btn btn-xs btn-default">
                            download  <code>.{{ format }}</code>
                        </a>
                        {% endfor %}
                    {% endif %}
                </div>
                {% if filter %}
                    <form action="" method="get" class="form form-inline">
                        {% bootstrap_form filter.form layout='inline' %}
			<div id="filtersubmit">
                        {% bootstrap_button 'filter' %}
			</div>
                    </form>
                {% endif %}
	    <form action="/plot_multitime/" method="post"> {% csrf_token %} 	
            <input type="submit" value="plot data">		    
                {% render_table table %}
	    </form>
	</div></div></div>
        {% endblock %}
    </div>
</body>
</html>
